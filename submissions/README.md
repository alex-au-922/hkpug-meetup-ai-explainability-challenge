# Participant Submissions

Open one long-lived PR and add files like this:

```text
submissions/AIEX-YOUR-TEAM/
  profile.json
  mission-01.json
  mission-02.json
  ...
```

Only JSON and Markdown files should go here. The GitHub Action scores the JSON
writeups and never runs participant code.
