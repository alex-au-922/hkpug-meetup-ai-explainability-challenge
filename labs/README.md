# Lab Artifacts

The workshop missions use small static artifacts instead of heavy cloud or GPU
setup. That keeps the first learning loop fast:

- read the artifact
- answer the mission question
- submit JSON
- let GitHub Actions score the evidence

The SHAP artifact is a simplified export from a loan-risk model. The Opik
artifact is an Opik-style trace export for a toy RAG support assistant.
